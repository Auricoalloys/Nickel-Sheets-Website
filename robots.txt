User-agent: *
Disallow: /cgi-bin/

# Template route - renders only with ?product=<slug>, nothing to index on its own.
Disallow: /pure-nickel-strip/product/

# /html/ holds the header, footer and product-page fragments that the client-side
# pages fetch at runtime. They are deliberately NOT blocked: Googlebot has to be
# able to load them, or it renders those pages without navigation or footer links.

Sitemap: https://www.nickelsheets.com/sitemap.xml
